# CLAUDE.md — Juancito Ads

## Al iniciar cada sesión en este proyecto

**Lee `docs/errors-learned.md` completo ANTES de tocar cualquier código.** Es la bitácora de fallas ya encontradas y cómo se resolvieron — el objetivo es no reincidir en errores ya conocidos. Cada vez que se corrija un bug, un fallo de build, o un comportamiento inesperado que haya requerido investigación, se añade una entrada nueva al final de ese archivo (nunca se sobreescriben las existentes). Formato de entrada:

```markdown
## [YYYY-MM-DD] — Título breve del error

**Contexto:** Qué se estaba construyendo o modificando.
**Error:** Mensaje exacto o descripción del síntoma.
**Causa raíz:** Por qué ocurrió.
**Fix aplicado:** Qué cambio exacto resolvió el problema.
**Prevención:** Regla o patrón a seguir en el futuro.
**Archivos:** `ruta/archivo.ts:L42`
```

## Proyecto

Sitio web de Juancito Ads — agencia de marketing digital en Panamá (Meta Ads + IA). Landing profesional multi-página, migrada desde un prototipo single-file HTML (`DEMO.html`, ya eliminado — ver `docs/superpowers/specs/2026-07-20-fase1-arquitectura-astro-design.md` para el diseño de la migración y `docs/superpowers/plans/2026-07-20-fase1-arquitectura-astro.md` para el plan de implementación).

**Stack:** Astro 5 + Tailwind CSS v4 (configuración CSS-first vía `@theme` en `src/styles/global.css` — **no** usar `tailwind.config.js/mjs`). Iconos SVG nativos vía `lucide-astro` (nunca emojis como ícono funcional). Sin framework de UI adicional (no React/Vue).

**Estructura:**
- `src/pages/` — 11 rutas: `/`, `/servicios`, `/servicios/campanas-ads`, `/servicios/campanas-redes`, `/servicios/paginas-web`, `/cotizador`, `/portafolio`, `/contacto`, `/privacidad`, `/terminos` y `/smartlink` (esta última **no se enlaza desde el sitio a propósito** — ver punto 14 del roadmap)
- `src/layouts/Layout.astro` acepta `bare` (sin navbar, footer ni chat) y `noindex`, para páginas que se consumen solas como `/smartlink`
- `src/components/` — componentes Astro, uno por sección/responsabilidad
- `src/data/site.ts` — **fuente única de verdad** para contacto, planes/precios, testimonios, portafolio. El número de WhatsApp y sus mensajes SIEMPRE se generan desde aquí (`waLink()`), nunca hardcodeados en un componente — incluye tanto el formato URL (`whatsappNumber`) como el formato visible (`whatsappDisplay`). Desde 2026-08-05 también contiene `planesCare`, `complementos` y `solucionesMedida` (antes hardcodeados dentro de `servicios/paginas-web.astro`) y todos los planes llevan `slug` estable.
- `src/data/footer.ts` — columnas y garantías del footer. Mismo motivo que `site.ts`: el contenido se edita en un sitio, el componente solo lo pinta.
- `src/data/cotizador.ts` — motor del cotizador: pasos, reglas y cálculo. **No inventa precios, los compone desde `site.ts`.** Es lógica pura sin DOM, y la importan tanto el render de Astro como el script del navegador — no hay dos copias del algoritmo.
- `src/layouts/Layout.astro` — layout base (navbar, footer, ChatWidget flotante, scroll-reveal)
- `public/` — assets estáticos (imágenes, videos, logo, favicons)
- `netlify/functions/` — Netlify Functions (backend serverless). Único archivo hoy: `chat.ts`, proxy del chatbot de soporte hacia la API de Groq — ver punto 4 del roadmap.

**Despliegue:** Netlify, trabajando principalmente en local. Repositorio: `git@github.com:juanarrietabusiness-pixel/PAGINA-JUANCITO-ADS.git` (colaborador vía cuenta de GitHub de MIPC — el repo es de `juanarrietabusiness@gmail.com`). El usuario conectó **manualmente** un proyecto de Netlify a este repo el 2026-07-20 — **proyecto real confirmado:** `juancitoads` en la cuenta de Netlify de `juanarrietabusiness@gmail.com` (URL en vivo: `https://juancitoads.netlify.app`; dashboard: `https://app.netlify.com/projects/juancitoads`; esta integración MCP de Netlify no tiene acceso a esa cuenta, cualquier cambio ahí requiere login manual del usuario o navegador). **Ojo:** existe un proyecto Netlify huérfano llamado `juancito-ads` (con guión, site id `9b6b1677-4d5b-471c-a8b1-31781ac5eb70`, en OTRA cuenta/team) creado por error vía MCP durante la sesión del 2026-07-20 — **no es** el sitio real en producción, quedó vacío/sin deploy. No reutilizarlo asumiendo que es el sitio live.

**Comandos:**
- `npm run dev` — servidor local (solo Astro, **no** ejecuta las Netlify Functions)
- `npm run build` — build de producción (criterio mínimo de aceptación para cualquier cambio: build sin errores)
- `npm run preview` — sirve el build de producción localmente
- `npm run check` — type-check (`astro check`); el `tsconfig.json` incluye `**/*`, así que también cubre `netlify/functions/`
- `npx netlify dev` — levanta Astro **y** las Netlify Functions juntos (normalmente en `http://localhost:8888`) — necesario para probar `netlify/functions/chat.ts` localmente, ya que `npm run dev` no las carga

## Reglas aprendidas (ver bitácora para el contexto completo)

- **Nunca usar `git add -A` / `git add .` / `git add --all`** en este proyecto — usar siempre rutas exactas. (Motivo: ver entrada del 2026-07-20 en `docs/errors-learned.md`.)
- Al centralizar cualquier dato de contacto/precio en `src/data/site.ts`, enumerar explícitamente todas sus representaciones necesarias (URL, texto crudo, texto de display) — no asumir que una forma cubre todos los usos.

## Fases pendientes del roadmap

1. ~~Fase 1 — Arquitectura Astro~~ ✅ completa (fusionada a `main`)
2. ~~Meta Pixel~~ ✅ instalado y **verificado en producción** (2026-07-20). `src/components/MetaPixel.astro` (commit `278f0a9`), env-configurable, solo dispara en build de producción. Se agregó `PUBLIC_FB_PIXEL_ID=1501740081256808` en el proyecto Netlify real `juancitoads` (Environment variables, todos los scopes) y se forzó un redeploy — confirmado con `curl` que el HTML servido en `https://juancitoads.netlify.app` incluye el script `fbevents.js` con el pixel ID correcto embebido. Ver entrada del 2026-07-20 en `docs/errors-learned.md` sobre por qué Meta Pixel Helper no lo detectaba pese a estar bien instalado.
3. ~~Rediseño visual "Tech-Neón Refinado"~~ ✅ completo y fusionado a `main` (2026-07-20, commits `84b64d0`..`1ce88af` + `3a111eb`). Auditoría de diseño → 3 direcciones estéticas propuestas → usuario eligió "Tech-Neón Refinado" → plan en `docs/superpowers/plans/2026-07-20-rediseno-tech-neon.md` (7 tareas) ejecutado vía subagentes con revisión por tarea + revisión final de rama. Cambios: tipografía Inter → Bricolage Grotesque (display) + Hanken Grotesk (body); utilidad `.atmosphere` (textura/gradiente) en `global.css`; cards diferenciadas por sección (ProblemaGrid con acento lateral naranja, ResultadosGrid con degradado, Metodologia con intensidad progresiva, Testimonios como bloque editorial sin caja); Portafolio con primera imagen en layout masonry 2x2; micro-interacciones de hover reforzadas en WhatsAppFloat/NavBar CTA/ContactForm (nota: `WhatsAppFloat.astro` existía en ese momento — se eliminó después, ver punto 4). Pusheado a producción — verificar visualmente en `https://juancitoads.netlify.app` una vez complete el deploy de Netlify.
4. ~~Chatbot de soporte con Groq (IA)~~ ✅ completo (2026-07-20). Spec en `docs/superpowers/specs/2026-07-20-chatbot-groq-design.md`, plan en `docs/superpowers/plans/2026-07-20-chatbot-groq.md` (5 tareas, ejecutado vía subagentes). **Reemplaza** el botón flotante de WhatsApp (`WhatsAppFloat.astro`, eliminado) por `ChatWidget.astro` — responde FAQ sobre planes/precios/resultados usando `llama-3.3-70b-versatile` de Groq vía `netlify/functions/chat.ts` (proxy server-side; `GROQ_API_KEY` nunca se expone al navegador, ver `.env.example`). El CTA de WhatsApp en `/contacto` (vía `CTAFinal.astro`) **no se tocó**, sigue igual; el chat también recomienda WhatsApp activamente y tiene un botón fijo hacia él. **Pendiente antes de que funcione en producción:** configurar `GROQ_API_KEY` en el dashboard de Netlify del proyecto `juancitoads` (Environment variables, sin prefijo `PUBLIC_`) — sin esa variable en el entorno de Netlify (no solo en el `.env` local), la Function responde `{"error":"server-misconfigured"}`. Sin rate-limiting por diseño (riesgo aceptado conscientemente, ver spec). Ver `docs/errors-learned.md` (entradas del 2026-07-20) sobre el bug de `@types/node`/rolldown y el riesgo de `taskkill /IM node.exe`.
5. ~~Análisis de opiniones UX/UI externas y plan de mejoras~~ — análisis en `docs/superpowers/specs/2026-07-21-mejoras-ux-analisis.md`. **Ítems 1-5 del plan ejecutados (2026-07-21):**
   - **Open Graph** ✅ — `astro.config.mjs` ahora define `site: "https://juancitoads.netlify.app"`; `Layout.astro` genera `og:*`/`twitter:*` + `<link rel="canonical">` a partir de `title`/`description`/`ogImage` (default `/logo.png`) de cada página, con URLs absolutas vía `Astro.site`.
   - **`ProblemaGrid` duplicado** ✅ — se quitó de `/servicios` (queda solo en Home; `/servicios` es `Metodologia` + `CTAFinal`).
   - **Scroll-reveal parpadeando en blanco** ✅ — confirmado visualmente con scroll rápido simulado (browser automation) antes del fix. Ajustar solo el `rootMargin` del `IntersectionObserver` (`Layout.astro`) **no fue suficiente** para saltos de scroll agresivos — el fix real fue subir el opacity inicial de `.reveal` de `0` a `0.4` en `global.css` (nunca queda completamente en blanco, incluso si el observer no llegó a disparar) + reducir duración/delays de la transición. Reverificado con el mismo método: ya no hay secciones en blanco.
   - **Hero saturado en mobile** ✅ — se quitó la fila logo+"Juancito Ads — Traffic Manager" de `Hero.astro` (redundante con el logo del navbar, en todos los breakpoints).
   - **"IA" como muletilla** ✅ — diferenciados los 3 badges idénticos "Optimizado con IA" en `Metodologia.astro` (ahora cada paso dice qué hace: generación de creativos, segmentación y presupuesto), subtítulos de `Metodologia.astro`/`Portafolio.astro` y las 3 líneas de `problemas` en `site.ts` reescritas para ser concretas en vez de genéricas.
   - **Prueba social (ítem 6)** — diferido explícitamente por el usuario, requiere datos reales de clientes (iniciales/rubro o captura de Ads Manager).
   - **Dominio propio, foto de equipo, preview de portafolio en home (ítem 7)** — pendientes de decisión de negocio/contenido, sin tocar.
   - **Tipografía de títulos (h1/h2/h3)** ✅ (2026-07-21, fuera del plan original, pedido aparte) — Bricolage Grotesque se sintió "infantil" para una agencia de Ads, especialmente en el H1 del Hero. Se armó un artifact comparando el H1 real en 6 tipografías sobre el fondo del sitio; el usuario eligió **Inter 900**. Cambio de un solo punto: `--font-display` en `global.css:13` pasó de `"Bricolage Grotesque"` a `"Inter"` (afecta a todo h1/h2/h3 vía la regla de `global.css:35-38`, no fue necesario tocar cada componente). El link de Google Fonts en `Layout.astro` ahora carga `Inter:wght@700;800;900` en vez de Bricolage Grotesque; Hanken Grotesk (body) no se tocó.
6. **Videos pesados sin resolver** — `public/videos/*.mp4` (53–71MB c/u, ~253MB total) siguen en el repo tal cual. El usuario decidió resolverlo más adelante (Google Drive o compresión). Bloqueó un intento de deploy vía herramienta MCP ad-hoc (ver bitácora) — el deploy nativo de Netlify vía Git sí lo maneja bien, así que no es urgente mientras el deploy sea vía Git-linked continuous deployment. Si se retoma: comprimir con ffmpeg (no instalado localmente al 2026-07-20) o mover a un host de video externo.
7. Fase 2 — SEO técnico (sitemap, robots.txt, schema.org, Open Graph). **Explícitamente pausada por el usuario** hasta resolver el tema de los videos — no iniciar sin que lo pida. (Nota: el Open Graph específicamente ya se identificó como quick-win aislado en el punto 5, no hace falta esperar a toda la Fase 2 para ese ítem puntual si el usuario lo pide antes.)
8. ~~Google Tag Manager~~ ✅ instalado (2026-07-21). `src/components/GoogleTagManager.astro` (mismo patrón que `MetaPixel.astro`: env-configurable vía `PUBLIC_GTM_ID`, solo dispara en build de producción), con dos `placement` (`"head"` — script principal, insertado lo más arriba posible en `<head>` — y `"body"` — `<noscript><iframe>`, insertado justo después de `<body>`, antes de `NavBar`). ID real (`GTM-WFWTVSKT`, provisto por el usuario) ya está en `.env` local.
   ~~Google Analytics 4 (gtag.js)~~ ✅ instalado también (2026-07-21, el usuario pasó el snippet directo de gtag.js aparte del GTM). `src/components/GoogleAnalytics.astro`, mismo patrón env-configurable vía `PUBLIC_GA_ID`, cableado en `Layout.astro` justo debajo de `<GoogleTagManager placement="head" />`. ID real `G-TF32ZWGF1Z` ya está en `.env` local. **Pendiente antes de que funcione en producción (ambos):** configurar en el dashboard de Netlify del proyecto `juancitoads` (Environment variables) `PUBLIC_GTM_ID=GTM-WFWTVSKT` **y** `PUBLIC_GA_ID=G-TF32ZWGF1Z`, luego forzar redeploy — mismo paso que se hizo con `PUBLIC_FB_PIXEL_ID` en el punto 2.
9. Fase 3 — GitHub Actions (tests en cada push) + previews automáticos de Netlify vía PR. Nota: el usuario ya conectó el repo a Netlify manualmente el 2026-07-20, así que los deploys automáticos a `main` probablemente ya funcionan sin GitHub Actions — falta solo la parte de tests en CI y previews por PR si se quiere ese flujo completo.
10. Esta bitácora y esta instrucción ya están activas desde ahora
11. **Optimización mobile** — pedido explícito del usuario (2026-07-21): "hay bastante que mejorar" en mobile, empezando por una sensación de tener que hacer zoom para leer. **Bug crítico encontrado y corregido:** `PaquetesGrid.astro` y `Portafolio.astro` tenían grillas con `grid-template-columns: repeat(N, 1fr)` fijo (4/3/3/3/2 columnas) sin ninguna variante responsive — en `/paquetes` (página de precios) esto forzaba 4 tarjetas en una fila en cualquier ancho de pantalla, aplastando el contenido en mobile. Reemplazado por clases Tailwind responsive (`grid-cols-1 sm:grid-cols-2 lg:grid-cols-4`, etc. — ver detalle en `docs/errors-learned.md`, entrada 2026-07-21). También se agrandó el tap target del botón de menú mobile en `NavBar.astro` (`p-1` → `p-2.5`, quedaba por debajo del mínimo de 44px recomendado). **Limitación de la sesión:** no se pudo verificar visualmente en viewport móvil real — `mcp__claude-in-chrome__resize_window` no tuvo efecto en este entorno (confirmado con `window.innerWidth` sin cambios tras el resize). El fix se validó por lectura del CSS compilado, no por captura de pantalla en mobile. **Sigue pendiente si se retoma:** verificación visual real en mobile (dispositivo físico o DevTools manual) — la emulación automatizada no funcionó en esta sesión.

**Segunda vuelta (mismo día, pedido explícito del usuario — "quiero que se sienta como una app nativa"):**
- **Pinch-zoom bloqueado a propósito → revertido el mismo día.** Se probó `maximum-scale=1.0, user-scalable=no` en `Layout.astro:27` tras decisión consciente del usuario (avisado el trade-off de accesibilidad/WCAG de antemano). **Rompió el sitio en Chrome mobile** (reportado por el usuario — Safari mobile se veía bien, Chrome mobile no; no se llegó a diagnosticar la causa exacta porque se priorizó revertir rápido dado que Chrome es el navegador mayoritario en Android). Revertido a `width=device-width, initial-scale=1.0` (el original). **No reintentar bloquear el zoom sin antes reproducir el problema en un Chrome Android real o emulado** — quedó sin diagnosticar qué combinación específica de propiedades del viewport causaba la rotura.
- **Auto-zoom de iOS en formularios (fix real y 100% efectivo, no depende del navegador):** iOS hace zoom automático al enfocar un `<input>`/`<textarea>` con `font-size` menor a 16px, sin importar el viewport meta. Los inputs de `ContactForm.astro` (nombre, negocio, whatsapp, mensaje) y el input del chat en `ChatWidget.astro` estaban en `text-sm` (14px) → subidos a `text-base` (16px).
- **Revisado y descartado como problema:** `Footer.astro` (nav flex-wrap, iconos sociales 36px — algo por debajo del ideal de 44px pero secundario/bajo impacto, no se tocó), `Testimonios.astro`/`ResultadosGrid.astro`/`CTAFinal.astro` (ya usaban `auto-fit, minmax()` o `flex-wrap`, responsive por naturaleza), `Lightbox.astro`/`VideoModal.astro` (ya usaban `max-w-[90vw]` y tap targets de 42-48px).

## Idioma

Responde siempre en español (heredado de la configuración global del usuario).

12. ~~Cotizador interactivo~~ ✅ completo (2026-08-05). Portado del sistema de `abrinay1997-stack/PanaClaw` (`src/pages/cotizador.astro` + `src/data/quote.ts` de ese repo) y **ampliado** para cubrir lo que PanaClaw no tiene: además de los planes web, cotiza los paquetes de campañas Meta Ads y de campañas + gestión de redes. Ruta `/cotizador`, enlazada desde NavBar, MobileDrawer, Footer y un CTA en `/servicios`.
    - **Qué se conservó de PanaClaw:** el precio se enseña ANTES de pedir los datos; cada opción muestra su coste antes de marcarla; total corriendo visible mientras se avanza; el plan recomendado es el más pequeño que cubra lo marcado (`requiresPlan`); capacidades que se reetiquetan a "Incluido" según el plan al que lleven las respuestas; y el envío arma un mensaje de WhatsApp con el resumen ya redactado.
    - **Qué cambió respecto a PanaClaw:** (a) **dos totales separados** — pago único (web y sus extras) y mensualidad (campañas, redes, mantenimiento, chatbot); PanaClaw solo maneja pagos únicos y sumar ambos daría una cifra falsa; (b) **pasos condicionales** (`showIf` con `anyOf`/`noneOf`): quien solo quiere anuncios ve 3 pasos, quien quiere todo ve 6; (c) elegir "campañas + redes" **anula** el cobro de "campañas de anuncios" (el plan de redes ya las incluye); (d) el navegador **importa el mismo módulo** que usó el build en vez de recibir el motor serializado en JSON y reimplementar el algoritmo — PanaClaw mantiene dos copias del cálculo a propósito, aquí no hace falta porque Astro bundlea el import del `<script>`.
    - **Verificado con Playwright** (Chromium headless, `npm run preview`): 4 flujos completos (solo web con capacidad incluida + extra + Care; solo ads; ads+redes+web; capacidad que sube el plan de Start a Corporate), totales y mensaje de WhatsApp correctos, 0 errores de JS, 0 desbordamiento horizontal a 390px, y capturas de viewport real en mobile y desktop. Es la primera vez en el proyecto que se logra verificación visual mobile real (ver la limitación registrada en el punto 11).
    - **Reglas de bloqueo (2026-08-05, pedido del usuario):** el cotizador apaga lo que ya no tiene sentido marcar, en vez de dejar que alguien crea que está añadiendo —y pagando— algo que ya tiene.
      1. **`ads` y `redes` son excluyentes.** Marcar "Campañas + gestión de redes" desmarca y bloquea "Campañas de anuncios" con la etiqueta "Ya incluido": el plan de redes ya trae la pauta de Meta. Está en los datos (`excluye: { valores, motivo }` en `QuoteOption`), no cableado a mano, para que añadir otra relación de este tipo sea una línea en `cotizador.ts`.
      2. **Las capacidades que el plan web ya cubre se bloquean y se atenúan** ("Salir en Google" y "Editarla tú mismo" en Corporate y Commerce).
      **Ojo con el bucle al tocar esto:** `blog` y `cms` suben el plan a Corporate *y a la vez* vienen incluidas en Corporate. Si el bloqueo mirase el plan final, se bloquearían solas → se desmarcarían → el plan bajaría → volverían a habilitarse, sin parar. Por eso `capacidadYaIncluida()` pregunta por el plan que exigen las **demás** respuestas (`planWebIgnorando()`), nunca el plan final. Hay un test que cubre justo ese caso (marcar `cms` sobre Start: `cms` sigue marcable, `blog` pasa a bloqueado, total $295 → $850, y al soltarlo vuelve todo atrás).
    - **Decisión pendiente:** el envío abre WhatsApp pero **no dispara ningún evento de conversión** (PanaClaw redirige a `/gracias` para que el píxel cuente un `Lead`). Este sitio no tiene página de gracias ni eventos de conversión cableados en ningún formulario, así que no se inventó uno solo para esta ruta — si se quiere medir conversiones, hay que decidirlo para todo el sitio a la vez (formulario de contacto, formulario de `/servicios/paginas-web` y cotizador).

13. ~~Hero, navbar, footer y páginas legales al estilo PanaClaw~~ ✅ completo (2026-08-05). Segunda tanda de portes desde `abrinay1997-stack/PanaClaw`, pedida por el usuario en la misma sesión que el cotizador.
    - **Sistema de botones** (`.btn` / `.btn-primary` / `.btn-ghost` / `.dot` en `global.css`): píldora blanca con punto naranja para la acción principal, contorno para la secundaria — calcado del sistema de PanaClaw, con los colores de Juancito. Disponible para todo el sitio, no solo para el hero.
    - **Hero** (`Hero.astro`): las 3 tarjetas de servicio se sustituyen por el par de botones (Cotizar mi campaña → `/cotizador`, Hablar por WhatsApp). Los 3 servicios siguen accesibles en una fila discreta de enlaces bajo los botones, además del navbar, `/servicios` y el footer. **Si se quiere recuperar las tarjetas, están en el historial del commit anterior a este.**
    - **Navbar que se encoge al bajar** (`NavBar.astro` + `#navbar.is-scrolled` en `global.css`): 74px → 58px de alto, `top` 24 → 10, ancho máximo 72rem → 64rem y logo 56 → 40px. Misma histéresis que PanaClaw (encoge pasados 40px de scroll, vuelve por debajo de 12px) para que no parpadee oscilando en el umbral. El menú móvil sigue al navbar vía `#navbar.is-scrolled ~ #navDrawer`. Las reglas cuelgan de `#navbar` a propósito: la especificidad de ID gana a las utilidades de Tailwind del propio elemento sin `!important`.
    - **Métrica de inversión gestionada**: `metricas.inversionGestionada` en `site.ts` (`+$40,000`), dato aportado por el usuario. Sale en el hero y también se le pasa al chatbot (`netlify/functions/chat.ts`) para que no invente otra cifra. **Al actualizarla, se cambia solo en `site.ts`.**
    - **Footer** (`Footer.astro` + `footer.ts`): estructura de PanaClaw — bloque superior con marca, tagline, pitch, 3 garantías con viñeta naranja y CTA al cotizador, más 4 columnas de enlaces (Servicios, Agencia, Legal, Contacto); bloque inferior con copyright, redes en botón redondo (hover naranja sólido) y enlaces legales. En móvil las columnas van a 2 y el pitch se oculta, igual que en PanaClaw y por la misma razón (apiladas ocupaban media pantalla de scroll de más).
    - **Páginas legales nuevas**: `/privacidad` y `/terminos`, con la tipografía de lectura larga en `.legal-body` (`global.css`) compartida por las dos. **No son plantillas genéricas:** la privacidad enumera los terceros reales del sitio (Meta Pixel, GA4, GTM, Netlify, Groq) y explica que el cotizador calcula en el navegador sin enviar nada; los términos recogen lo que el sitio ya promete (presupuesto de ads aparte, cifras del cotizador orientativas, 50/50 en web, código a tu GitHub, dominio y hosting del primer año). **Si entra otro tercero o cambia una condición comercial, hay que actualizar esas páginas en el mismo commit.** Pendiente de revisión del usuario: política de cancelación de planes mensuales y de reembolsos — se redactó lo más conservador posible a partir de lo que el sitio ya decía, pero no venía de una decisión suya explícita.
    - **Verificado con Playwright:** encogido del navbar medido en los tres estados (arriba / con scroll / de vuelta arriba), menú móvil sin solaparse con el navbar encogido, 0 errores de JS, 0 desbordamiento horizontal a 390px en footer y páginas legales, y las 3 rutas nuevas respondiendo 200.

14. ~~Smartlink (`/smartlink`)~~ ✅ completo (2026-08-05). El enlace único para la bio de Instagram y TikTok, portado del `/smark` de PanaClaw. Tarjeta de vidrio centrada sobre las partículas, con logo, tagline, tres credenciales calculadas (ubicación, `metricas.inversionGestionada` y el precio del plan de Ads más barato, sacado de `planesMetaAds` — **no** escrito a mano), cuatro destinos (Calcular mi precio → `/cotizador` en naranja sólido, WhatsApp con mensaje abierto que termina en "Mi negocio es: ", Servicios y Portafolio), redes y salida al sitio completo.
    - **No está enlazada desde ninguna parte del sitio, y es a propósito:** no va en el navbar ni en el footer, y lleva `noindex, nofollow` para que Google no la trate como una puerta alternativa. Se llega solo pegando la URL en la bio. (Hay un test que verifica que la portada no la enlaza.)
    - **Regla al añadir botones: no, o casi.** La página tiene un solo trabajo, repartir hacia un destino; cada botón nuevo reparte peor los clics entre todos los demás. Si entra un destino, que salga otro. Y el texto se mide en palabras: se llega desde el navegador interno de Instagram, con una mano y sin paciencia — una etiqueta que se parte en dos líneas deja de leerse como botón (por eso el `@media (max-width: 360px)` recorta relleno y tamaño de fuente antes que el texto).
    - Para que exista sin navbar ni footer, `Layout.astro` estrenó los props `bare` y `noindex`.
    - **Efecto colateral corregido:** `ParticleCanvas.astro` buscaba su contenedor por `document.getElementById("hero")`, así que fuera de la portada nunca arrancaba el bucle de dibujo y el lienzo quedaba en blanco sin ningún error. Ahora se apoya en `canvas.parentElement` y funciona en cualquier sección. Ver la entrada del 2026-08-05 en `docs/errors-learned.md`.
    - **Verificado con Playwright** a 320, 390 y 1280px: sin navbar/footer/chat, `robots` en `noindex, nofollow`, las 4 etiquetas en una sola línea en los tres anchos, 0 desbordamiento horizontal, 0 errores de JS, y el lienzo de partículas pintando de verdad (contando píxeles con `getImageData()`, no mirando la captura).

15. ~~Navbar circular, portafolio por categorías, página de Ayuda, fondos ambientales y limpieza de copy~~ ✅ completo (2026-08-05). Tanda pedida por el usuario en un solo bloque de nueve puntos.
    - **Navbar en píldora** (`NavBar.astro`): `rounded-2xl` → `rounded-full`, para que case con el botón "Cotizar" del hero. El CTA "Quiero crecer" y el botón del menú móvil pasaron también a `rounded-full`, y el cajón móvil a `rounded-[28px]`. **Ojo:** el menú de escritorio pasó de `md:` a `lg:` — con seis destinos más el CTA ya no cabían a 768px sin apretar el texto. Si se añade un séptimo enlace, no cabe: hay que quitar uno o mover el CTA al cajón.
    - **Portafolio reconstruido por categorías** — era el punto que el usuario marcó como más crítico. `Portafolio.astro` pasó de ser un archivo de 240 líneas con todo dentro a un orquestador con portada (título, índice de categorías y tres cifras) más tres componentes: `PortafolioWebs.astro` (01 — los seis sitios reales, cada uno en una tarjeta con barra de navegador y su dominio visible), `PortafolioCasos.astro` (02 — los resultados que estaban en la portada, ahora con el resultado como primera línea en grande) y `PortafolioCreativos.astro` (03 — gráficas en mosaico por columnas y videos, con el lightbox y el modal de video que antes vivían en `Portafolio.astro`). `ResultadosGrid.astro` se eliminó: `PortafolioCasos` lo reemplaza.
    - **Sitios web reales** en `sitiosWeb` (`site.ts`): StemFlow, LiveSync Pro, Acústica Superior, BukoFlow Store, Baby Caleb y Feria del Lente, con URL pública, rubro, descripción y qué se construyó. **La prueba es el enlace**, por eso no lleva métricas: cualquiera puede abrirlo. Al añadir un proyecto, la cifra "N sitios en producción" de la portada del portafolio se actualiza sola.
    - **Marcas ficticias eliminadas** — la sección "Marcas que Confían en Nosotros" listaba seis nombres inventados como si fueran clientes. Ver la entrada del 2026-08-05 en `docs/errors-learned.md`. **No reponerla sin nombres reales y permiso del cliente.**
    - **Página `/ayuda`** con 27 preguntas en cuatro temas (Antes de empezar · Precio y pagos · Durante el proyecto · Después de la entrega), en `src/data/ayuda.ts`. Acordeón `<details>`/`<summary>` nativo: sin JavaScript, con teclado por defecto y el buscador del navegador encuentra texto dentro. **Regla al escribir una respuesta: no se estrena ninguna promesa ahí** — cada una tiene que poder rastrearse a la página de un plan, a `/terminos` o a `/privacidad`; si hace falta una condición nueva, se decide primero en `/terminos` y luego se refleja en la ayuda, nunca al revés. Enlazada desde navbar, cajón móvil y footer (dos veces: columna Agencia y columna Contacto).
    - **Fondos ambientales** (`Fondo.astro`) — las cuatro imágenes que subió el usuario, en `public/fondos/`, al 4,5–8 % de opacidad con desenfoque y máscara radial. No pintan color propio, así que el mismo componente sirve sobre `bg-deep` y sobre `bg-alt`. Puestos en portafolio (las tres categorías y la portada), `/ayuda`, `/contacto`, `/servicios` y las tres páginas de servicio. **Por encima de ~0,12 de opacidad el texto deja de leerse:** el tope está documentado en el propio componente. La sección contenedora necesita `relative overflow-hidden` y su contenido `relative z-[1]`.
    - **Assets reorganizados en `public/`** — `public/portafolio/webs/` (capturas de los seis sitios), `public/portafolio/creativos/` (las tres gráficas, antes en `public/images/`, carpeta ya eliminada) y `public/fondos/`. **Todo convertido a WebP con `sharp`: 4,6 MB → 380 KB** sin pérdida visible (los fondos van desenfocados al 6 %). Los originales PNG/JPG no se conservaron en el repo. Nada que ver con el problema de los videos del punto 6, que sigue igual: `public/videos/*.mp4` siguen pesando ~253 MB.
    - **`/contacto` a dos columnas** — "¿Cómo te gustaría empezar?" a la izquierda y el formulario a la derecha, dentro de una misma sección. `ContactForm.astro` dejó de traer su propio `<section>` y su fondo: ahora es un bloque, porque era ese envoltorio lo que lo obligaba a ir debajo. Se añadió una cuarta tarjeta a la izquierda que lleva al cotizador.
    - **Copy limpiado** — el pitch del footer perdió "Todo desde Panamá, 100 % remoto."; se quitó la línea "Panamá · 100 % remoto" del footer y las siete viñetas "100% remoto" de los planes de Ads y Redes. **La ubicación sigue en `site.ubicacion`** y se usa en el copyright, en `/terminos`, en `/privacidad` y en `/smartlink` — lo que se quitó es la muletilla repetida, no el dato.
    - **Portada** — al mudarse los resultados al portafolio, entra `TrabajosPreview.astro` en su lugar: tres capturas de sitios reales que enlazan a `/portafolio#webs`. Elige los proyectos por `slug`, así que reordenar `sitiosWeb` no cambia lo que sale en la portada.
    - **Verificado con Playwright** (Chromium, `npm run preview`) a 1440px y 390px: navbar confirmado como píldora midiendo `border-radius` contra la altura real, 27 preguntas con 4 abiertas de entrada y el acordeón abriendo al clic, los 6 dominios reales enlazados, 14/14 imágenes cargadas en ambas resoluciones, 0 desbordamiento horizontal, 0 errores de JS y "100 % remoto" ausente de todas las páginas. `npm run build` (12 páginas) y `npm run check` (0 errores) limpios. **Antes de capturar hay que recorrer la página:** ver la entrada del 2026-08-05 sobre `loading="lazy"` en la bitácora.
    - **Pendiente de decisión del usuario:** los videos del portafolio no tienen `poster`, así que dependen de que el navegador pinte un fotograma desde `preload="metadata"`; si no lo hace, se ven como cajas vacías con el botón de play.

16. ~~Selector del hero, fin del difuminado morado, navbar por página y datos de campañas al día~~ ✅ completo (2026-08-14). Tanda pedida por el cliente y su jefe en un solo bloque.
    - **Hero con selector "¿Qué estás buscando?"** (`Hero.astro`) — era el punto marcado como de máxima prioridad. Los dos botones (cotizador + WhatsApp) se sustituyen por una pregunta y tres tarjetas, una por servicio. La línea grande de cada tarjeta está escrita **en lo que quiere el cliente** ("Quiero que me lleguen clientes") y el nombre del servicio va debajo en pequeño; cada una enseña su precio de entrada, calculado con `precioDesde()` desde los propios arrays de planes. Cotizador y WhatsApp bajan a una línea secundaria, como respuesta al "no sé cuál necesito". **Tres opciones es el techo, no una casualidad:** si algún día entra un cuarto servicio, se agrupan dos o se manda a `/servicios` — no se añade una cuarta tarjeta.
    - **Se acabó el morado** — no venía del tema sino de `formas-3d.webp`, un render de burbujas moradas que `Fondo.astro` fundía con `mix-blend-mode: screen` sobre secciones enteras. Ver la entrada del 2026-08-14 en `docs/errors-learned.md`. `Fondo.astro` **eliminado** junto con sus doce llamadas; las cuatro imágenes de `public/fondos/` quedan sin uso (tres de ellas no son publicables nítidas: cifras inventadas, marca de un tercero y texto ilegible).
    - **`SeccionMedia.astro`** (nuevo) — el patrón que pidió el cliente tomando PanaClaw de referencia: texto a un lado, **imagen opaca y nítida** al otro, con el aire de PanaClaw (`py-20 sm:py-28 lg:py-32`, texto a 46ch). Prop `lado` para alternar. En móvil el texto va siempre primero. Usado en la portada (`tienda-01`), en campañas de Ads (`feria-01`) y en campañas + redes (`panales-01`) — los tres son creativos reales de clientes reales. **Regla del componente: solo entran imágenes cuyo contenido pueda leerse sin problema**, porque nítidas se leen.
    - **Navbar que marca la página actual** (`NavBar.astro`, `MobileDrawer.astro`, `src/data/nav.ts` nuevo) — "Cotizador" iba en naranja fijo en todas las páginas y se leía como "estás aquí". Ahora los destinos salen de `navLinks` (una sola lista para escritorio y móvil) y el activo se decide con `esRutaActiva()` + `aria-current="page"`, resaltado con píldora clara en escritorio y barra naranja en el cajón. Una ruta hija marca a su padre (`/servicios/campanas-ads` enciende "Servicios"). El acento naranja vuelve a significar acción, no ubicación.
    - **Precios "Desde" en los tres servicios** (`servicios.astro`) — campañas y campañas + redes llevaban una etiqueta de categoría mientras web enseñaba "Desde $295". Las tres salen ahora de `precioDesde()`, así que no pueden quedarse viejas.
    - **Datos de campañas y redes refrescados** desde `Agencia_Workspace/Juancito Ads/01_ADN_y_Memoria/01_brand_guidelines.md` (sección 6, cuadro del fundador del 13-08-2026). Lo más desactualizado eran los planes de redes: Arranque pasa a **2 redes** (era 1), Crecimiento a **3 redes con TikTok básico y 2 jornadas** (eran 2 redes y 1 visita) y Escala a **TikTok avanzado con 4 jornadas** (eran 2 visitas); en Ads entran "estrategia centrada en ventas", "multicampaña" y los Reels estratégicos. Se propagó a `cotizador.ts` (las tres opciones de contenido) y a `ayuda.ts` (dos respuestas hablaban de "una visita en Arranque y Crecimiento, dos en Escala"). El chatbot (`netlify/functions/chat.ts`) no hizo falta tocarlo: arma su conocimiento desde `site.ts`.
    - **Portafolio** — fuera las tres gráficas (siguen en el sitio, pero grandes y junto al texto que explican, vía `SeccionMedia`). Los videos se rehicieron: el problema no era el estilo sino el encuadre, porque `video-01` es un reel vertical 1080×1920 al que la parrilla 16:9 le cortaba la cabeza y los pies. Ahora la retícula es asimétrica —el vertical a su 9:16 real en una columna alta, los tres horizontales al lado, el último a doble ancho para que no quede hueco— y cada tarjeta lleva degradado propio, etiqueta de formato y play enmarcado. `Lightbox.astro` y `portafolioImagenes` quedaron sin uso y se eliminaron.
    - **Portada** — entra `SeccionMedia` y sube `Metodologia` (la fórmula "Oferta + Contenido + Campaña = Ventas", que es la metodología real del fundador según el ADN) desde `/servicios/campanas-redes`, donde solo la veía quien ya había elegido ese plan. Responde al pedido de que se entiendan servicios **y** estrategias.
    - **Verificado con Playwright** (Chromium, `npm run preview`) a 1440px y 390px sobre las 9 rutas: **0 elementos con `mix-blend-mode: screen` y 0 imágenes con `blur`** (la comprobación de que el morado no puede volver), `aria-current` correcto en las 9, 0 desbordamiento horizontal, 0 errores de JS y todas las imágenes cargando. `npm run build` (12 páginas) y `npm run check` (0 errores) limpios.
    - **Pendiente y conocido:** los videos siguen sin `poster`. No se pudo generar en esta sesión —ni el ffmpeg recortado del entorno ni el Chromium sin códecs propietarios pueden abrir los archivos, que además son QuickTime con extensión `.mp4`— así que la tarjeta se diseñó para sostenerse sin fotograma. **Hacerlo junto con la compresión del punto 6**, en una máquina con ffmpeg completo.

17. ~~Base negra y escenas a sangre al estilo PanaClaw~~ ✅ completo (2026-08-14). Segunda vuelta sobre el punto 16, tras revisión del cliente.
    - **El morado que quedaba era el propio fondo del tema.** Medido con Playwright + `sharp`: 91 % de los píxeles en 210-240° (azul), 0,2 % en la banda violeta. No había morado en el CSS. Lo que había era `--color-bg-deep: #050D1F`, un azul marino con 26 puntos de diferencia entre R y B, que al lado del naranja y comparado con un negro real se percibe violáceo. La base baja a `#05070C` / `#0A0D14` (6 puntos), el footer a `#030408` y los radiales de `.atmosphere` a 0,055/0,04. **El techo para el fondo son ~10 puntos de diferencia entre R y B**; por encima vuelve la lectura violácea. El azul sigue en el sitio, pero como acento, no como suelo.
    - **`SeccionMedia.astro` rehecho como escena, no como tarjeta.** La primera versión ponía la imagen en una caja con borde al lado del texto; PanaClaw la pone **a sangre** en una sección de `min-height: 88vh`, atenuada sobre negro y bajo un velo doble (horizontal que abre carril de lectura, vertical que funde con las secciones vecinas). Ahora `lado` indica **dónde va el texto** —la imagen se lee en el lado contrario— y `foco` coloca el motivo en el lado libre. Sigue sin haber `blur` ni `mix-blend-mode`: solo `opacity` sobre negro, que únicamente puede restar, así que ninguna imagen puede teñir una sección.
    - **Ojo con las imágenes verticales:** con `object-fit: cover` en un marco apaisado, `object-position` en X no hace nada (la imagen se escala por el ancho); solo la Y mueve el encuadre. Ver la entrada del 2026-08-14 en `docs/errors-learned.md`.
    - **Verificado con Playwright** a 1440px y 390px en las 9 rutas: 0 `mix-blend-mode: screen`, 0 imágenes con `blur`, `aria-current` correcto, 0 desbordamiento horizontal, 0 errores de JS. El footer pasó de 22.413 píxeles con color perceptible a 821. `npm run build` y `npm run check` limpios.

18. ~~Hero a la izquierda, azul corregido por matiz, imágenes del cliente y Bot multicanal~~ ✅ completo (2026-08-14). Tercera vuelta, con el cliente eligiendo entre opciones presentadas.
    - **El azul, resuelto por fin — y la lección es el matiz.** Las dos vueltas anteriores trataron el morado como cuestión de *cantidad* de azul (bajar la diferencia R-B, luego irse a negro). El problema era el **matiz**: el azul de siempre estaba en 221°, la frontera del índigo, que junto al naranja se lee violáceo. La base pasa a `#050F1A` / `#08182A`, ambos en **211°** — mismo azul marino, desplazado al cian. **Regla: el fondo se mantiene por debajo de ~215°.** Ver la entrada del 2026-08-14 en `docs/errors-learned.md`.
    - **Footer azul con halo frío.** `#030A12` (mismo matiz que el cuerpo, un punto más oscuro) y el halo superior pasa de naranja a azul. Un acento cálido extendido sobre una superficie grande deja de ser acento y se vuelve fondo sucio. Los acentos naranja puntuales (viñetas, CTA) se conservan.
    - **Hero rediseñado — el cliente eligió entre tres opciones** presentadas en un artifact con las tres renderizadas a tamaño real. Ganó la 2: titular, subtítulo y métrica **alineados a la izquierda**, y el selector como pregunta + tres pastillas horizontales de tres palabras cada una (etiqueta, servicio en pequeño, precio). **Los tamaños del móvil están medidos, no elegidos:** las tres pastillas tienen que entrar sin desplazar a 390×844 y a 375×667 (hay una `@media (max-height: 740px)` en `global.css` para pantallas bajas). A 360×640 la tercera asoma parcialmente — apretar más comprometía la legibilidad. **Si se toca el copy del hero, volver a medirlo.**
    - **Imágenes del cliente** en `public/fondos/` — seis, convertidas a WebP (10,56 MB → 528 KB) y renombradas sin caracteres raros: `circuitos-centro` (centro despejado, la mejor para secciones centradas), `circuitos-derecha`, `circuitos-diagonal`, `red-esferas` (azul **y** naranja de marca), `escritorio-3d` (megáfonos, el símbolo del logo) y `laptop-pregunta`. **`laptop-pregunta` lleva su propio titular escrito**, así que no vale como fondo genérico: solo donde ese mensaje sea el mensaje (se usa en `/servicios/paginas-web`). Las cuatro imágenes viejas se eliminaron.
    - **`FondoEscena.astro`** (nuevo) — el hermano de `SeccionMedia` para secciones centradas (cierre CTA, portada de servicios, `/ayuda`, `/cotizador`, `/contacto`, portada del portafolio), que estaban planas sobre color liso. Mismo principio irrenunciable: **`opacity` sobre el fondo, nunca `mix-blend-mode` ni `blur`**. Velo radial (no lineal) porque el contenido va en el centro.
    - **Chat naranja y con latido.** El botón flotante pasa de azul a naranja, igual que el CTA del navbar y el del footer: el naranja es el color de "esto se toca" en todo el sitio. Late cada 9 s, con movimiento solo durante ~1,4 s — una animación continua se vuelve invisible en segundos y molesta el resto de la visita. Se detiene al pasar el ratón, al abrirlo y con `prefers-reduced-motion`.
    - **Bot multicanal, $499** — cuarto servicio, equivalente al eBot de PanaClaw pero con contenido propio (`botMulticanal` en `site.ts`). Va **abajo en `/servicios`**, no como cuarta tarjeta arriba: las tres de arriba son las puertas de entrada del negocio y una cuarta repartiría la atención donde hay que decidir. **Los costes que no cobra la agencia —alojamiento y llave de IA, ambos a nombre del cliente— van publicados con el precio**, no en letra chica. Enlazado desde el footer (`/servicios#bot-multicanal`) y dentro del conocimiento del chatbot.
    - **Verificado con Playwright** a 1440px y 390px en las 9 rutas: **0 errores**, 0 `mix-blend-mode: screen`, 0 imágenes con `blur`, `aria-current` correcto, 0 desbordamiento horizontal y todas las imágenes cargando. `npm run build` y `npm run check` limpios.

19. ~~Bot con página propia, bug del cotizador y fondos más presentes~~ ✅ completo (2026-08-14). Correcciones sobre el punto 18, reportadas por el cliente desde el preview.
    - **Bug: el fondo tapaba el texto de `/cotizador`.** `FondoEscena` es `absolute` y en CSS eso se pinta por encima de cualquier contenido no posicionado, aunque lleve `z-0`. Al contenedor del cotizador le faltaba `relative z-[1]` y la página entera se veía lavada. **Ahora hay una prueba en el verificador que lo caza en todas las rutas** — mide el brillo del píxel de cada `h1`/`h2`, porque `elementFromPoint` no sirve (ignora `pointer-events: none`). Ver la entrada del 2026-08-14 en `docs/errors-learned.md`: incluye la lección de que **una prueba nueva no vale hasta verla fallar**.
    - **El Bot pasa a ser el cuarto servicio con página propia** (`/servicios/bot-multicanal`), no una sección al final de `/servicios`. La rejilla de servicios va a **2×2** (`md:grid-cols-2`): con cuatro tarjetas de este peso, cuatro columnas dejan ~210px útiles y el texto se parte en tiras. La página propia tiene hero con precio, los cuatro canales (con el aviso del trámite de WhatsApp, la única sorpresa posible en la entrega), qué hace, y un bloque dedicado a **los dos costes que no cobra la agencia**.
    - **Imágenes de fondo más presentes** — el cliente pidió "un poquito más": las intensidades suben ~40 % (0,18–0,3 → 0,28–0,4). El tope sigue siendo la legibilidad del texto; la prueba de brillo de titulares es ahora la red de seguridad.
    - **Fuera `tienda-01` de la portada.** Era la imagen que el cliente marcó como la que menos le gustaba. La escena de la portada usa `red-esferas`. Los otros dos creativos reales (`feria-01` en Ads, `panales-01` en redes) se mantienen: son prueba de trabajo hecho.
    - **Verificado con Playwright** en 10 rutas × 2 viewports: **0 errores**, incluida la prueba nueva de titulares. `npm run build` (13 páginas) y `npm run check` limpios.
